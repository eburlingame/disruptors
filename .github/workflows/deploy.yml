name: Build and deploy

on:
  push:
    branches: [master]

  workflow_dispatch:

env:
  API_IMAGE: ${{ github.repository }}/disruptors/disruptors-client:sha-${{ github.sha }}
  CLIENT_IMAGE: ${{ github.repository }}/disruptors/disruptors-api:sha-${{ github.sha }}

  LATEST_API_IMAGE: ${{ github.repository }}/disruptors/disruptors-client:sha-latest
  LATEST_CLIENT_IMAGE: ${{ github.repository }}/disruptors/disruptors-api:sha-latest

jobs:
  backend-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub container registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push tag container
        uses: docker/build-push-action@v2
        with:
          context: ./backend
          file: ./backend/Dockerfile
          tags: ${{ env.API_IMAGE }}

  backend-test:
    runs-on: ubuntu-latest
    needs: [backend-build]
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub container registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run pytest
        run: |
          cd backend/
          docker pull $IMAGE
          docker run $IMAGE sh -c "pytest"

  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to GitHub container registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push tag container
        uses: docker/build-push-action@v2
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.prod
          registry: docker.pkg.github.com
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          tags: ${{ env.CLIENT_IMAGE }}

  deploy:
    needs:
      - backend-build
      - backend-test
      - frontend-build

    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Push latest container images
        run: |
          echo "Push api container to latest tag"
          docker pull $API_IMAGE
          docker push $LATEST_API_IMAGE

          echo "Push client container to latest tag"
          docker pull $CLIENT_IMAGE
          docker push $LATEST_CLIENT_IMAGE

      - name: Trigger the deployment pipline
        run: |
          curl --request POST \
          --form "token=${{ secrets.DEPLOY_TRIGGER_TOKEN }}" \
          -F "variables[ANSIBLE_PLAYBOOK]=disruptors.deploy.yml" \
          --form ref=master "${{ secrets.DEPLOY_TRIGGER_URL }}"
